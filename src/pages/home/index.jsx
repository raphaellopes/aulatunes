import React from 'react';

// Move to a wrapper component
import { Header, Container } from '../../components';

const Home = () => (
  <>
    <Header />
    <Container>
      <h1>Home page</h1>
    </Container>
  </>
);

export default Home;
